{
  "name": "mtrade",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create mtrade-db",
    "db:migrate:local": "wrangler d1 migrations apply mtrade-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply mtrade-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
